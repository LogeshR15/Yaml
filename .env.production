# Production environment
# Catalyst Zoho Application ID (Console → Settings → Environments → General).
# Differs from development. Used for the embedded Zoho sign-in that gates YAML download.
VITE_CATALYST_ZAID=50043352518
